import path from "path";

export const CORE_PROTO_PATH = path.join(__dirname, "core", "core.proto");

export const AUTH_PROTO_PATH = path.join(__dirname, "auth", "auth.proto");

export const ADMISSION_PROTO_PATH = path.join(
  __dirname,
  "admission",
  "admission.proto"
);

export const MESSAGE_PROTO_PATH = path.join(
  __dirname,
  "message",
  "message.proto"
);
