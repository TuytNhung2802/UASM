export enum EROLE {
  USER = "user",
  AMDIN = "admin",
}

export enum ELEVEL {
  UNIVERSITY = "university",
  AFTER_UNIVERSITY = "after_university",
}

export enum EFILE {
  DOCX = "docx",
  PDF = "pdf",
  TXT = "txt",
  XLSX = "xlsx",
}

export enum EMessageType {
  AI = "ai",
  USER = "user",
  STAFF = "staff",
}
