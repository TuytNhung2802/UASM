export enum EMessageType {
  AI = "ai",
  USER = "user",
}

export enum EFILE {
  DOCX = "docx",
  PDF = "pdf",
  TXT = "txt",
  XLSX = "xlsx",
}

export enum ELEVEL {
  UNIVERSITY = "university",
  AFTER_UNIVERSITY = "after_university",
}
